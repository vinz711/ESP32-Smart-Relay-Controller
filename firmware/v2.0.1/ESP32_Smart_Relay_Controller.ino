// ESP32 Smart Relay Controller v2.0.1
// Baseline firmware migrated from the tested R3 / OTA implementation.
//
// The complete tested firmware source will be inserted here from the
// verified baseline after source synchronization. Application-specific
// behavior will be separated in a future refactoring commit without
// changing the tested behavior in this baseline.

#include <Arduino.h>

void setup() {
  // v2.0.1 baseline entry point.
}

void loop() {
  // v2.0.1 baseline loop.
}
